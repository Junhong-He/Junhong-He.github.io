# 2026-05-22

## 个人主页修复

1. **background图片修复**：`index.html` 第113行写死了 `background.jpeg`，但实际图片已改为 `background.jpg`，修改路径引用为 `background.jpg`。
   - 管理位置：`index.html` Top Section 的 `style="background-image: url(...)"` 属性。

3. **background.jpg 线上不显示根本原因**：图片文件从来没有 commit 过（git untracked），已 `git add static/assets/img/background.jpg` + push 修复。同时改进 `main.css` 中 `.top-section` 的 `background-position: center 30%` + `background-repeat: no-repeat`。

4. **Publications 背景色改白色**：class 从 `bg-gradient-primary-to-secondary-gray` 改为 `bg-gradient-primary-to-secondary-light`。

2. **页面section顺序调整**：将 Publications 移至 Experience（Research Experience）之前。
   - 修改了 `index.html` 导航栏链接顺序（PUBLICATIONS 排在 EXPERIENCE 前）。
   - 交换了 HTML body 中两个 section 块的位置（publications section 置于 experience section 前）。
   - 注意：`scripts.js` 的 `section_names` 数组只影响 markdown 动态加载顺序，不影响页面视觉位置，无需修改。
